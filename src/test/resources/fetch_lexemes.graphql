query Lexemes($filters: [LexemesFilter!]){
    lexemes(filters: $filters){
        headword
        language {
            shortIsoCode
            name
        }
        senses {
            ordinal
            value
        }
    }
}